//primitive data types 

let name = "jhon "; // string 
let age = 24; // number 
let ismarried = false; // boolean //it give true or false value 
let salary = null; // null // it is a sipecial value that represents the absence of any value 
let address; // undefiend // it is a special value that represents the absence of any value 
//let sym =symbol ("unique"); // symbol // it is a special value that represents a unique value.
let bigIntValue = BigInt(1234567899); //Bigint // it is a special value that represent a large integer value that is larger than the maximum safe in javascript.
//console.log ( typeof name );// it will give string 


//Non proimitive data types 
// Object in js 
const item ={
    age:24,
    name:"julia",
    ismarried:false,
    address:undefined,
    salary:null, 
}
console.log(item['name'])// it will give julia 
